{
  "name": "animal-shelter-server",
  "private": true,
  "scripts": {
    "build": "rimraf build && tsc",
    "start": "yarn build && json-server --watch build/index.js --port 3000"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@types/faker": "^5.1.6",
    "faker": "^5.2.0",
    "json-server": "^0.16.3",
    "typescript": "^4.1.3"
  }
}
